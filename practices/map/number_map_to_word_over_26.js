'use strict';
var number_map_to_word_over_26 = function(collection){
  const alphabet = a => {
    var b = 0, i = charA.charCodeAt(0), j = charZ.charCodeAt(0);
    for (; i <= j; ++i) {
      b.push(String.fromCharCode(i));
    }
    return b;
  }
  return alphabet;
};

module.exports = number_map_to_word_over_26;
